<%= form_for(@enrolled) do |f| %>
  <% if @enrolled.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enrolled.errors.count, "error") %> prohibited this enrolled from being saved:</h2>

      <ul>
      <% @enrolled.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="field">-->
  <!--  <%#= f.label :student %><br>-->
  <!--  <%#= f.text_field :student_id %>-->
  </div>
  <!--<div class="field">-->
  <!--  <%#= f.label :course %><br>-->
  <!--  <%#= f.text_field :course_id %>-->
  <!--</div>-->
  <div class="field">
    <%= f.label :zID %><br>
    <%= f.collection_select :student_id,Student.all,:id,:user_id %><br/>
  </div>
  
  
  <div class="field">
    <%= f.label :courses %><br>
    <%= f.collection_select :course_id,current_user.courses,:id,:name %><br/>
  </div>
  
  <div class="field">
    <%= f.label :Status %><br>
    <!--<= f.number_field :event_type %>-->
    <%= f.select(:status_type, Enrolled.status_types.keys.map {|key| [key.humanize, key]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
